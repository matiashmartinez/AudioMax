<Window x:Class="AudioTwin.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AudioTwin Pro v1.2.0" Height="650" Width="900" Background="Black" ResizeMode="CanMinimize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="AudioTwin Pro v1.2.0" Foreground="#DEFF9A" FontSize="28" FontWeight="Bold" Margin="0,0,0,15" Grid.Row="0"/>

        <!-- Entrada -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Dispositivo de entrada (Cable Virtual):" Foreground="White" FontSize="14"/>
            <ComboBox x:Name="InDev" Height="35" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Salidas -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Altavoces de salida físicos (Sincronización y Volumen):" Foreground="White" FontSize="14" Margin="0,0,0,5"/>
            <ListView x:Name="OutDevs" Height="170" Background="#1E1E1E" Foreground="White" BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Usar" Width="50">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Dispositivo de Audio" Width="400" DisplayMemberBinding="{Binding FriendlyName}"/>

                        <GridViewColumn Header="Retardo (ms)" Width="130">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding DelayMs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="80" HorizontalAlignment="Left" Background="#2D2D2D" Foreground="#DEFF9A" FontWeight="Bold" TextAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Volumen (%)" Width="130">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Volume, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="80" HorizontalAlignment="Left" Background="#2D2D2D" Foreground="#DEFF9A" FontWeight="Bold" TextAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>

        <!-- Switch Auto-Sync -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,15" VerticalAlignment="Center">
            <CheckBox x:Name="ChkAutoSync" IsChecked="True" Content=" Activar Auto-Sincronización por silencio (Anti-Desfase Bluetooth)" Foreground="#DEFF9A" FontSize="14" FontWeight="Bold"/>
        </StackPanel>

        <!-- Botones -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="▶ Aplicar" Click="Apply_Click" 
                    Background="#DEFF9A" Foreground="Black" Height="40" Width="120" FontSize="16" FontWeight="Bold" Margin="0,0,10,0"/>
            <Button Content="⏹ Detener" Click="Stop_Click" 
                    Background="#FF5555" Foreground="White" Height="40" Width="120" FontSize="16" FontWeight="Bold"/>
        </StackPanel>

        <!-- Logs -->
        <StackPanel Grid.Row="5">
            <TextBlock Text="Registro de actividad:" Foreground="White" FontSize="14" Margin="0,0,0,5"/>
            <ListBox x:Name="LogList" Background="#1E1E1E" Foreground="#B0B0B0" 
                     FontFamily="Consolas" FontSize="12" Height="90" 
                     HorizontalContentAlignment="Stretch" BorderThickness="0"/>
        </StackPanel>

        <!-- Estado -->
        <StatusBar Grid.Row="6" Background="#2D2D2D" Foreground="#DEFF9A">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Listo."/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>