<Window x:Class="AudioTwin.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AudioTwin Pro" Height="550" Width="850" Background="Black" ResizeMode="CanMinimize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="AudioTwin Pro" Foreground="#DEFF9A" FontSize="28" FontWeight="Bold" Margin="0,0,0,15" Grid.Row="0"/>

        <!-- Entrada -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Dispositivo de entrada (Cable Virtual):" Foreground="White" FontSize="14"/>
            <ComboBox x:Name="InDev" Height="35" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Salidas -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Altavoces de salida (múltiples):" Foreground="White" FontSize="14"/>
            <ListBox x:Name="OutDevs" Height="120" SelectionMode="Multiple" FontSize="13" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Botones -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="▶ Aplicar" Click="Apply_Click" 
                    Background="#DEFF9A" Foreground="Black" Height="40" Width="120" FontSize="16" FontWeight="Bold" Margin="0,0,10,0"/>
            <Button Content="⏹ Detener" Click="Stop_Click" 
                    Background="#FF5555" Foreground="White" Height="40" Width="120" FontSize="16" FontWeight="Bold"/>
        </StackPanel>

        <!-- Panel de logs -->
        <StackPanel Grid.Row="4">
            <TextBlock Text="Registro de actividad:" Foreground="White" FontSize="14" Margin="0,0,0,5"/>
            <ListBox x:Name="LogList" Background="#1E1E1E" Foreground="#B0B0B0" 
                     FontFamily="Consolas" FontSize="12" Height="150" 
                     HorizontalContentAlignment="Stretch" BorderThickness="0"/>
        </StackPanel>

        <!-- Estado -->
        <StatusBar Grid.Row="5" Background="#2D2D2D" Foreground="#DEFF9A">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Listo."/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>